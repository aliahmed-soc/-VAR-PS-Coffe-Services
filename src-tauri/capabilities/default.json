{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: invoke only. No filesystem or shell.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
